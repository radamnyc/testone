<%= render 'account/shared/page' do |page| %>
  <% page.title t('.section') %>
  <% page.body do %>
    <%= cable_ready_updates_for @cohort_subscription do %>
      <%= render 'account/shared/box', divider: true do |box| %>
        <% box.title t('.header') %>
        <% box.description do %>
          <%= t('.description') %>
          <%= t('.manage_description') if can? :manage, @cohort_subscription %>
        <% end %>

        <% box.body do %>
          <% with_attribute_settings object: @cohort_subscription, strategy: :label do %>
            <%= render 'shared/attributes/belongs_to', attribute: :cohort %>
            <%= render 'shared/attributes/option', attribute: :state_of_interest %>
            <%= render 'shared/attributes/number', attribute: :amount %>
            <%# 🚅 super scaffolding will insert new fields above this line. %>
          <% end %>
        <% end %>

        <% box.actions do %>
          <%= link_to t('.buttons.edit'), [:edit, :account, @cohort_subscription], class: first_button_primary if can? :edit, @cohort_subscription %>
          <%# 🚅 super scaffolding will insert new action model buttons above this line. %>
          <%= button_to t('.buttons.destroy'), [:account, @cohort_subscription], method: :delete, class: first_button_primary, data: { confirm: t('.buttons.confirmations.destroy', model_locales(@cohort_subscription)) } if can? :destroy, @cohort_subscription %>
          <%= link_to t('global.buttons.back'), [:account, @project, :cohort_subscriptions], class: first_button_primary %>
        <% end %>

        <% box.raw_footer do %>
          <%# 🚅 super scaffolding will insert new action model index views above this line. %>
        <% end %>
      <% end %>
    <% end %>

    <%# 🚅 super scaffolding will insert new children above this line. %>
  <% end %>
<% end %>
